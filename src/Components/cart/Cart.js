import React from 'react';

const cart = (props) => {
    return (
        <div>
            order summery
             <p> Product Name:{addToCart}</p>

        </div>
    );
};

export default cart;